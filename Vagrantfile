# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|
  # note that this is for m1 macs. If you have an intel, use a different box
  config.vm.box = "bento/ubuntu-22.04-arm64"
  config.vm.hostname = 'wordpress-setup'
end
